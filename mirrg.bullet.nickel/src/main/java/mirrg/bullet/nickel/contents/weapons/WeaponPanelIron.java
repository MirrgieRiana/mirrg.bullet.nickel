package mirrg.bullet.nickel.contents.weapons;

public class WeaponPanelIron// implements IWeapon
{
	/*
		private int age = 0;
	
		@Override
		public void moveEnemy(ILiving living, PhaseBattle phase)
		{
	
			// ランダム板
			if (age % 5 == 0) {
				double theta = Math.random() * 2 * Math.PI;
				double speed = Math.random() * 0.005 + 0.002;
	
				BulletStraight bullet = new BulletStraight(
					living.getX() + (Math.random() * 2 - 1) * living.getRadius() * 2,
					living.getY() + (Math.random() * 2 - 1) * living.getRadius() * 2,
					speed * Math.cos(theta),
					speed * Math.sin(theta),
					0.002,
					Color.lightGray,
					100);
				phase.addBulletEnemy(bullet);
	
				int count = 4 + (int) (5 * Math.random());
				for (int i = 0; i < count; i++) {
					phase.addBulletEnemy(new BulletStraight(
						bullet.x + i * 0.01 * Math.cos(theta + Math.PI / 2),
						bullet.y + i * 0.01 * Math.sin(theta + Math.PI / 2),
						speed * Math.cos(theta),
						speed * Math.sin(theta),
						0.002,
						Color.lightGray,
						100));
				}
			}
	
			age++;
		}
	*/
}
