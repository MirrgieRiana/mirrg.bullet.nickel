package mirrg.bullet.nickel.weapons;

public class WeaponBulletsGold// implements IWeapon
{
	/*
		private int age = 0;
	
		@Override
		public void moveEnemy(ILiving living, PhaseBattle phase)
		{
	
			// 自機狙い3Way
			if (age % 20 == 0) {
				for (int i = 0; i < 3; i++) {
	
					BulletStraight bullet = new BulletStraight(living.getX(), living.getY(), 0, 0, 0.010, Color.orange, 100);
					double theta = Math.atan2(phase.player.getY() - bullet.y, phase.player.getX() - bullet.x)
						+ (i - 1) * 3 * Math.PI / 180;
					double speed = 0.015;
					bullet.xx = speed * Math.cos(theta);
					bullet.yy = speed * Math.sin(theta);
	
					phase.addBulletEnemy(bullet);
				}
			}
	
			age++;
		}
	
		@Override
		public void movePlayer(ILiving living, PhaseBattle phase)
		{
			// TODO 自動生成されたメソッド・スタブ
	
		}
	*/
}
