package mirrg.bullet.nickel.contents.weapons;

public class WeaponScatterLaserEmerald// implements IWeapon
{
	/*
		private int age = 0;
	
		@Override
		public void moveEnemy(ILiving living, PhaseBattle phase)
		{
	
			for (int i = 0; i < 4; i++) {
				for (int j = 0; j < 8; j++) {
					BulletStraight bullet = new BulletStraight(living.getX(), living.getY(), 0, 0, 0.005, Color.green, 50);
					double theta = (i + 1) * age * 4.2 * Math.PI / 180
						+ j * 0.4 * Math.PI / 180
						+ 90 * Math.PI / 180;
					double speed = 0.012 + 0.0002 * j;
					bullet.xx = speed * Math.cos(theta);
					bullet.yy = speed * Math.sin(theta);
	
					phase.addBulletEnemy(bullet);
				}
			}
	
			age++;
		}
	
		@Override
		public void movePlayer(ILiving living, PhaseBattle phase)
		{
			// TODO 自動生成されたメソッド・スタブ
	
		}
	*/
}
